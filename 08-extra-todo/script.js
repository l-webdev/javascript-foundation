const todos = ["CSS lernen", "JS lernen"];

function listTodos() {
  console.log(todos);
}

listTodos();
// Ausgabe:
// "CSS lernen"
// "JS lernen"

function addTodo(todo) {}

function deleteTodo(todo) {
  todos.splice(a, b);
  console.log(todos);
}
